<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.mytourguide">

    <application
        android:allowBackup="true"
        android:icon="@drawable/thessaloniki1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CityInfoActivity" />
        <activity android:name=".CitySongsActivity" />
        <activity android:name=".WhereToGoActivity" />
        <activity android:name=".WhatToEatActivity" />
        <activity android:name=".ArrangeActivity"></activity>
    </application>

</manifest>